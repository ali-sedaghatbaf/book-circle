{
  "name": "monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@monorepo/web",
    "build": "npm run build --workspace=@monorepo/web",
    "start": "npm run start --workspace=@monorepo/web",
    "lint": "npm run lint --workspace=@monorepo/web"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "prettier": "^3",
    "eslint": "^8",
    "eslint-config-next": "14.2.3"
  }
}
